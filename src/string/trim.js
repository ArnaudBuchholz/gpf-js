/**
 * @file Helper to trim strings
 * @since 0.1.6
 */
/*#ifndef(UMD)*/
"use strict";
/*exported _gpfStringTrim*/ // Trim the string
/*#endif*/

/**
 * Trim the string
 *
 * @param {String} that String to trim
 * @return {String} Trimmed string
 * @since 0.1.6
 */
function _gpfStringTrim (that) {
    return that.trim();
}
